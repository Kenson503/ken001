// Make sure Typed.js is loaded before this script runs.
// If using a module system, import it like this:
// import Typed from 'typed.js';

// If using a script tag, ensure this script runs after including Typed.js

var typed = new Typed(".text", {
    strings: ["Web Developer", "Frontend Developer", "Graphic Designer", "Backend Developer", "JavaScript Developer"],
    typeSpeed: 100,
    backSpeed: 100,
    backDelay: 1000,
    loop: true
});